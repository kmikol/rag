# Workflow: Update Docs

1. Identify whether the change belongs in a module README, MkDocs page, ADR, or all three.
2. Keep planned and implemented behavior clearly separated.
3. Update `docs/mkdocs.yml` if new pages are added.
4. Run `mkdocs build -f docs/mkdocs.yml --strict`.
