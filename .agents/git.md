# Git Rules

- Keep commits scoped to the requested change.
- Do not commit generated `site/`, caches, `.env.local`, or secrets.
- Do not rewrite history unless explicitly asked.
- Do not revert user changes without permission.
- Before finishing, check `git status --short --branch`.
